# Get stated
1. type in CMD the folowing ling `git clone https://github.com/alon4551/Branded_API.git`
2. type `cd /Branded_API`
3. type `npm install`
4. type `npm start`
5. server will listen to port 3000